extend layout

block content
  h1 uploader

  if message
    div #{message}

  form(action='/register' method='POST' enctype='multipart/form-data')
    div upload the image.
    input(type='file' name='image' accept='image/*' required)
    
    div submit the image.
    input(type='submit' value='submit')

